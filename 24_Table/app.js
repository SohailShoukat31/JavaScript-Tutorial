// Table

for (var table = 1; table <= 10; table++) {
  console.log(2 + "x" + table + "=" + 2 * table + "<br>");
}

var userValue = prompt("Enter Here Desire Table Number :");
for (var i = 1; i <= 10; i++) {
  document.write(userValue + " x " + i + " = " + userValue * i, "<br>");
}
